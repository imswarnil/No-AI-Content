node_modules/
.next/
out/
.env
.env.*
!.env.example
.vercel
*.tsbuildinfo
next-env.d.ts
.DS_Store

# Cloudflare / OpenNext
.open-next/
.wrangler/
# Local secrets for `wrangler dev` and the OpenNext preview — same values as
# .env, in the format wrangler expects. Never commit.
.dev.vars
cloudflare-env.d.ts
